class Solution:
    def isAnagram(self, s: str, t: str) -> bool:
        #here sorting the strings according to ASCII values
        #since after sorting both strings will be in the same order
        s="".join(sorted(s))
        t="".join(sorted(t))
        if s==t:
            return True
        else:
            return False
